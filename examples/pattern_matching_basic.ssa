module @pattern_matching_basic
  
export function @classify_number(@n: int) -> atom {
    @entry:
    match @n { 0: @zero, 1: @one, _: @other }
  
    @zero:
    ret atom zero
  
    @one:
    ret atom one
  
    @other:
    ret atom other
}
